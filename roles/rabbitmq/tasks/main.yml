---
- name: Install RabbitMQ and configure
  import_tasks: install_rabbitmq.yml

- name: Create RabbitMQ cluster
  import_tasks: join_cluster.yml
  when: rabbitmq_mode == cluster
  tags:
    - join_cluster